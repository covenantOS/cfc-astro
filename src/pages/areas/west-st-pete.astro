---
import AreaLanding from '~/components/AreaLanding.astro';
import { getAreaPageConfig } from '~/data/area-pages';

const config = getAreaPageConfig('west-st-pete')!;
---

<AreaLanding config={config} />
